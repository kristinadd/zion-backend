services:
  # PostgreSQL database
  db:
    image: postgres:18
    container_name: zion-backend-db
    environment:
      POSTGRES_USER: zion_backend
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD:-secure_password_change_in_production}
      POSTGRES_DB: zion_backend_production
    volumes:
      - postgres_data:/var/lib/postgresql
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U zion_backend -d postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Rails application
  web:
    build: .
    container_name: zion-backend-web
    ports:
      - "3000:80"
    environment:
      - RAILS_MASTER_KEY=${RAILS_MASTER_KEY}
      - DATABASE_HOST=db
      - DATABASE_USERNAME=zion_backend
      - DATABASE_PASSWORD=${DATABASE_PASSWORD:-secure_password_change_in_production}
      - DATABASE_PORT=5432
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data:

